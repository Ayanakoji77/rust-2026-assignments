# Solution notes: Character frequency, sorted

## Approach

It is simple approach of adding to the hasmap the char if present increase the value if not add the key. Simple as that.
For sorting using ths inbuilt sort function that takes the function and returns an ordering based on that.

# Edge cases handled

## Anything special

There would me more than one way to handle this thing using the inbuilt entry or something function present.
