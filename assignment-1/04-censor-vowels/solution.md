# Solution notes: Censor vowels in place

## Approach

My approch uses the std library function to change the characters using the map function and match use case to \* , safe then the direct covert to byte and change in place. This uses more memory approch not optimized then the bytes approach.

## Edge cases handled

## Anything special
